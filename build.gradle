plugins {
  id "org.sonarqube" version "3.1.1"
}

sonarqube {
  properties {
    property "sonar.projectKey", "tonyleiva_spring-gateway-patterns"
    property "sonar.organization", "tonyleiva"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}
